{
  "mcpServers": {
    "wordpress": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://${WP_SITE_URL}/wp-json/easy-mcp-ai/v1/mcp",
        "--header",
        "Authorization: Bearer ${WP_API_TOKEN}"
      ]
    }
  }
}
