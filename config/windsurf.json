{
  "mcpServers": {
    "wordpress": {
      "serverUrl": "https://${WP_SITE_URL}/wp-json/easy-mcp-ai/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${WP_API_TOKEN}"
      }
    }
  }
}
